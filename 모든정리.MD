# JS 전체 치트시트 (Day 01 ~ 07)

한 줄 요약본. 자세한 건 각 폴더 `정리.md` 참고.

---

## Day 01 — 기초문법

| 문법 | 설명 |
|---|---|
| `const x = 1` | 값 못 바꿈. **기본은 이걸로** |
| `let x = 1` | 값 바꿀 수 있음 |
| `var` | 옛날 것. 쓰지 말 것 |
| `` `안녕 ${name}` `` | 백틱. 문자열 안에 변수 끼워넣기 |
| `typeof x` | 자료형 확인 → `"string"` `"number"` `"object"` |
| `===` / `!==` | **타입까지** 비교. 항상 이걸 쓸 것 |
| `==` | 타입을 멋대로 맞춤. `1 == "1"` 이 true |
| `Number("5")` | 문자열 → 숫자 |
| `String(5)` | 숫자 → 문자열 |

---

## Day 02 — 조건문 / 반복문

| 문법 | 설명 |
|---|---|
| `if (조건) {} else {}` | 조건 분기 |
| `조건 ? A : B` | 삼항. 한 줄짜리 if/else |
| `for (let i=0; i<n; i++)` | n번 반복 |
| `for (const x of arr)` | 배열 **값**을 하나씩 |
| `for (const k in obj)` | 객체 **키**를 하나씩 |
| `arr.forEach(x => {})` | 배열 돌기. **반환값 없음** |
| `break` / `continue` | 멈추기 / 이번 회차만 건너뛰기 |

**falsy 6개** — `false` `0` `""` `null` `undefined` `NaN` (나머지는 전부 truthy)

---

## Day 03 — 함수

| 문법 | 설명 |
|---|---|
| `function f() {}` | 선언식. 호이스팅 됨 |
| `const f = function () {}` | 표현식 |
| `const f = () => {}` | 화살표. **기본은 이걸로** |
| `const f = x => x * 2` | 한 줄이면 `{}` 와 `return` 생략 |
| `const f = (a, b = 10) => {}` | 기본값 매개변수 |
| `return` | 값을 돌려주고 **함수 종료** |
| `return;` 생략 | `undefined` 가 나옴 |

- **매개변수**(parameter) = 받는 자리 / **인자**(argument) = 실제로 넣는 값
- **스코프** — `{}` 안에서 만든 변수는 밖에서 못 봄
- **콜백** — 함수를 **인자로 넘기는 것**. `arr.map(f)` 의 `f`
- ⚠️ `f` = 함수 자체 / `f()` = 지금 실행

---

## Day 04 — 배열

| 메서드 | 돌려주는 것 | 원본 |
|---|---|---|
| `arr.push(x)` / `pop()` | 길이 / 뺀 값 | **바뀜** |
| `arr.unshift(x)` / `shift()` | 길이 / 뺀 값 | **바뀜** |
| `arr.map(x => ...)` | 새 배열 (**개수 같음**) | 그대로 |
| `arr.filter(x => 조건)` | 새 배열 (**개수 줄어듦**) | 그대로 |
| `arr.find(x => 조건)` | **요소 하나** (없으면 `undefined`) | 그대로 |
| `arr.findIndex(...)` | 인덱스 (없으면 `-1`) | 그대로 |
| `arr.includes(x)` | `true/false` | 그대로 |
| `arr.indexOf(x)` | 인덱스 (없으면 `-1`) | 그대로 |
| `arr.join(", ")` | 문자열 | 그대로 |
| `str.split(",")` | 배열 | — |
| `arr.slice(1, 3)` | **잘라온 사본** (끝 미포함) | 그대로 |
| `arr.concat(b)` | 이어붙인 새 배열 | 그대로 |
| `arr.sort((a,b) => a-b)` | 정렬된 배열 | **바뀜** |
| `arr.reverse()` | 뒤집힌 배열 | **바뀜** |

- ⚠️ `sort()` 는 그냥 쓰면 **문자열로 정렬**함. `10` 이 `9` 보다 앞에 옴
- ★ `a - b` = 오름차순(작은 것부터) / `b - a` = 내림차순
- 원본 바꾸는 것: `push pop shift unshift splice sort reverse` → 복사부터: `[...arr]`

## Day 04 — 객체

| 문법 | 설명 |
|---|---|
| `{ key: value }` | 기본 형태 |
| `obj.name` / `obj["name"]` | 읽기. 변수로 키를 쓸 땐 대괄호 |
| `obj.age = 21` | 추가 / 수정 (같은 문법) |
| `delete obj.age` | 삭제 |
| `obj?.a?.b` | **옵셔널 체이닝**. 중간이 없어도 에러 안 남 |
| `Object.keys(obj)` | 키 배열 |
| `Object.values(obj)` | 값 배열 |
| `Object.entries(obj)` | `[[k,v], [k,v]]` |
| `const { a, b } = obj` | **구조 분해**. 꺼내기 |
| `const [x, y] = arr` | 배열도 구조 분해 됨 |
| `{ ...obj, age: 22 }` | 스프레드 복사 + 덮어쓰기 |
| `JSON.stringify(obj)` | 객체 → 문자열 |
| `JSON.parse(str)` | 문자열 → 객체 |

---

## Day 05 — 배열·객체 심화

| 문법 | 설명 |
|---|---|
| `arr.reduce((acc, cur) => ..., 초깃값)` | 배열을 **값 하나로 접기** |
| `arr.some(x => 조건)` | **하나라도** 맞으면 true |
| `arr.every(x => 조건)` | **전부** 맞아야 true |
| `A ?? B` | A가 `null`/`undefined` 면 B |
| `A \|\| B` | A가 **falsy** 면 B (`0` `""` 도 걸림) |
| `{ name, age }` | 단축 속성. `{name: name}` 의 줄임 |
| `{ [키변수]: 값 }` | **계산된 키**. 대괄호 없으면 글자 그대로 키가 됨 |
| `const { a: { b } } = obj` | 중첩 구조 분해 (파고들기) |
| `const { a: newName } = obj` | 이름 바꾸기 |
| `const { a = "기본" } = obj` | 기본값. **`undefined` 에만 걸림** (`null` 은 통과) |
| `arr.splice(2, 1)` | (시작, **개수**) 지우기. **원본 바뀜** |
| `arr.splice(1, 0, x)` | 안 지우고 끼워넣기 |
| `Object.fromEntries(쌍배열)` | `entries` 의 반대. 배열 → 객체 |

**reduce 핵심** — 화살표가 **반환한 값이 다음 회차의 `acc`**. 회차 내내 **타입을 바꾸지 말 것**
**초깃값 규칙** — 항상 쓸 것. 단 "요소 중 하나 고르기"(최댓값)는 예외로 생략

```js
arr.reduce((acc, cur) => acc + cur.price, 0)                    // 합계
arr.reduce((acc, cur) => cur.price > acc.price ? cur : acc).name // 최댓값 → 이름
arr.reduce((acc, cur) => ({ ...acc, [cur.name]: cur.price }), {}) // 객체 만들기
Object.fromEntries(Object.entries(o).map(([k,v]) => [k, v*1.1])) // 객체 가공
```

**`??` vs `||`**

| 값 | `??` | `\|\|` |
|---|---|---|
| `undefined` `null` | 기본값 | 기본값 |
| `0` `""` `false` `NaN` | **원래 값** | 기본값 |

---

## Day 06 — DOM

### lib.js
```js
const $  = e => document.querySelector(e)            // 하나 (없으면 null)
const $$ = e => [...document.querySelectorAll(e)]    // 전부 (진짜 배열)
const nl = (a,t) => Object.assign(document.createElement(a), t)  // 요소 만들기
```

| 문법 | 설명 |
|---|---|
| `$("#id")` `$(".cls")` `$("p.cls")` | CSS 선택자 그대로 |
| `부모.querySelector(".x")` | **그 요소 안에서만** 찾기 |
| `el.textContent` | 글자 (읽기/쓰기). **기본은 이것** |
| `el.innerHTML` | HTML로 해석. ⚠️ 사용자 입력엔 쓰지 말 것 |
| `el.value` | input 의 **현재** 입력값 |
| `el.getAttribute("href")` | HTML에 **적힌 글자 그대로** |
| `el.dataset.name` | `data-name` 속성. ⚠️ **항상 문자열** |
| `el.dataset.userName` | `data-user-name` → 카멜케이스 |
| `el.classList.add("x")` | 클래스 붙이기 |
| `el.classList.remove("x")` | 떼기 |
| `el.classList.toggle("x")` | 있으면 떼고 없으면 붙이기 (불리언 반환) |
| `el.classList.contains("x")` | `true/false` |
| `el.className = "x"` | ⚠️ 통째로 덮어씀. 기존 클래스 날아감 |
| `el.style.backgroundColor` | **카멜케이스**. `background-color` 안 됨 |
| `el.style.width = "200px"` | ⚠️ **단위 필수**. `200` 은 조용히 무시됨 |
| `el.parentElement` | 부모 |
| `el.nextElementSibling` | 다음 형제 |
| `el.previousElementSibling` | 이전 형제 |
| `el.children` | 자식 전부 (**배열 아님** → `[...]`) |
| `el.closest(".card")` | **자기부터 위로** 올라가며 첫 조상 |

**함정 3개**
1. `$()` 못 찾으면 **`null`** → `?.` 로 막기
2. `$$()` 는 **배열** → `$$(".x").textContent = "y"` 는 **조용히 무시됨**. `forEach` 로 돌려야 함
3. `querySelectorAll` 은 NodeList → `.map()` 없음. `$$` 는 `[...]` 라서 됨

**최종 형태**
```js
$$(".card")
  .filter(c => Number(c.querySelector(".price").textContent) > 50000)
  .forEach(c => c.classList.add("red"))
```

---

## Day 07 — 이벤트

| 문법 | 설명 |
|---|---|
| `el.addEventListener("click", () => {})` | 리스너 등록 |
| `el.addEventListener("click", f, { once: true })` | 한 번만 |
| `el.removeEventListener("click", f)` | 떼기. **같은 함수**를 넘겨야 함 |
| `e.target` | **실제로 눌린** 요소 (제일 안쪽) |
| `e.currentTarget` | **리스너가 붙은** 요소 |
| `e.type` | `"click"` 등 이벤트 이름 |
| `e.key` | 눌린 키 → `"Enter"` `"Escape"` |
| `e.target.checked` | 체크박스 상태 (`value` 아님!) |
| `e.preventDefault()` | 브라우저 기본 동작 막기 |
| `e.stopPropagation()` | 버블링 멈추기 |
| `el.append(x)` / `prepend(x)` | 맨 뒤 / 맨 앞에 붙이기 |
| `el.remove()` | 자기 자신 삭제 |
| `nl("li", { textContent: "x" })` | 요소 만들기 |

**이벤트 종류**

| 이름 | 언제 |
|---|---|
| `click` | 눌렀을 때 |
| `input` | 입력값 바뀔 때마다 (**글자 하나하나**) |
| `change` | 포커스 떠날 때 / select·checkbox는 즉시 |
| `submit` | form 제출 (**form 에 붙일 것**) |
| `keydown` | 키 누를 때 |
| `DOMContentLoaded` | HTML 다 읽었을 때 |

**함정 3개**
1. ⚠️ `addEventListener("click", f())` — 괄호 붙이면 **즉시 실행**되고 등록은 안 됨
2. ⚠️ `$$(".btn").addEventListener(...)` — 배열엔 없음. `forEach` 로 돌려야 함
3. ⚠️ form 에서 `preventDefault()` 빼면 **페이지가 새로고침**돼서 콘솔이 지워짐

**버블링** — 이벤트는 안 → 밖으로 퍼짐. `inner → middle → outer`

**이벤트 위임** ⭐ — 부모 하나에만 붙이면 **나중에 생긴 자식도 동작**
```js
list.addEventListener("click", (e) => {
  const btn = e.target.closest(".del")
  if (!btn) return              // ← 위임의 첫 줄은 거의 항상 이것
  btn.closest("li").remove()
})
```

**this** — 일반 함수에선 `currentTarget`, 화살표 함수엔 **없음**(window).
→ 그냥 `e.currentTarget` 쓸 것

---

## 자주 틀리는 것 모음

| | 틀린 것 | 맞는 것 |
|---|---|---|
| 함수 넘기기 | `addEventListener("click", f())` | `addEventListener("click", f)` |
| 여러 개 | `$$(".x").classList.add()` | `$$(".x").forEach(e => ...)` |
| 요소 → 글자 | `map(e => e.querySelector(".p"))` | `...textContent` 까지 |
| 문자열 → 숫자 | `dataset.price + 1000` → `"50001000"` | `Number(...) + 1000` |
| reduce 타입 | `? cur.name : acc` (타입 섞임) | `? cur : acc` 하고 밖에서 `.name` |
| 붙이기 vs 확인 | `classList.contains("x")` | `classList.add("x")` |
| 단위 | `style.width = 200` | `style.width = "200px"` |
| 이상/초과 | "이상"에 `>` | "이상" = `>=`, "초과" = `>` |
| console.log 위치 | `console.log($("#x"))?.textContent` | `console.log($("#x")?.textContent)` |

---

## PHP 대응표 (Day 09 대비)

| JS | PHP |
|---|---|
| `arr.map(f)` | `array_map($f, $arr)` |
| `arr.filter(f)` | `array_filter($arr, $f)` |
| `arr.reduce(f, 0)` | `array_reduce($arr, $f, 0)` |
| `arr.includes(x)` | `in_array($x, $arr)` |
| `arr.join(",")` | `implode(",", $arr)` |
| `str.split(",")` | `explode(",", $str)` |
| `Object.keys(o)` | `array_keys($o)` |
| `JSON.stringify(o)` | `json_encode($o)` |
| `JSON.parse(s)` | `json_decode($s, true)` |
| `a ?? b` | `$a ?? $b` (동일) |
